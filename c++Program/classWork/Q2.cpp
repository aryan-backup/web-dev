//? WAP to find the sum of two numbers by using two number 

#include<iostream>
using namespace std;

#define SUM(a,b)(a+b)

int main(){
  int l1=10, l2=5,sum;

  sum=SUM(l1,l2);
  cout<<"The sum is "<<sum;

  return 0;
}